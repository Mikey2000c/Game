{
  "name": "spinkeep",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix server & npm run dev --prefix mockup -- --host 0.0.0.0 --port 5173",
    "dev:api": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix mockup -- --host 0.0.0.0 --port 5173",
    "install:all": "npm install --prefix server && npm install --prefix mockup"
  }
}
