{
  "name": "spinkeep",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix mockup",
    "dev:api": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix mockup",
    "dev": "npm run install:all && npm run dev:api & npm run dev:web",
    "lint": "npm run lint --prefix mockup",
    "build": "npm run build --prefix server && npm run build --prefix mockup"
  }
}
